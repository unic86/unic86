### Hi there 👋
## This is a easy project in python. it is Guess the numbers.

پیشنهاد دوم: سیستم مدیریت «زنجیره تأمین هوشمند قطعات استراتژیک» (Smart Supply Chain)
این پروژه روی مدیریت هوشمند مواد اولیه و قطعات بین یک «هلدینگ صنعتی»، «تأمین‌کنندگان» و «واحدهای تولیدی» تمرکز دارد.a

Host (تأمین‌کننده قطعات/انباردار):

Inventory Management: افزودن قطعات جدید با مشخصاتی مثل (نام، تعداد، قیمت واحد، زمان تخمینی تحویل).
پنل سفارشات: مشاهده درخواست‌های خرید و تغییر وضعیت آن‌ها به «ارسال شده» یا «لغو شده».
گزارش فروش: مشاهده کل درآمد کسب شده از استارتاپ‌های مختلف.

موجودی لحظه‌ای قطعات صنعتی (مثلاً بلبرینگ، سنسور، آی‌سی) را وارد می‌کند.
وضعیت لجستیک و ارسال (در راه، تحویل شده) را آپدیت می‌کند.
برای قطعات کمیاب، «مزایده» برگزار می‌کند.

User (مدیر خط تولید استارتاپ):
Marketplace: مشاهده لیست قطعات موجود در انبار تمام Hostها.
ثبت سفارش: انتخاب قطعه و تعداد، و پرداخت از طریق اعتبار (Credit).
درخواست بودجه: اگر اعتبار یوزر کافی نبود، درخواستی برای Admin ارسال کند تا اعتبارش را افزایش دهد.

نیاز واحد تولیدی خود را ثبت می‌کند (مثلاً: نیاز به ۱۰ عدد سنسور تا فردا).
در صورت کمبود بودجه، درخواست وام داخلی از Admin می‌دهد.
گزارش مصرف قطعات را برای جلوگیری از هدررفت ثبت می‌کند.

Admin (مدیر لجستیک و سرمایه‌گذار):
مدیریت کاربران: تأیید صلاحیت استارتاپ‌ها و تأمین‌کنندگان برای ورود به چرخه.
مانیتورینگ بحران: مشاهده لیست قطعاتی که موجودی آن‌ها در کل شبکه به سطح خطر رسیده است.
تأیید تراکنش‌های سنگین: اگر هزینه یک سفارش از حد مجاز بالاتر بود، نیاز به تایید ادمین داشته باشد
تعیین سقف سفارش برای هر User.
مشاهده نمودار مصرف قطعات در کل سیستم (تفکر آماری).
تایید پرداخت‌های سنگین مالی بین User و Host.

بخش خلاقانه: می‌توانید سیستمی طراحی کنید که اگر موجودی انبارِ یک Host به زیر ۵ عدد رسید، سیستم به طور خودکار به Admin هشدار «وضعیت بحرانی» بدهد (اتوماسیون در خدمت انسان).
چه چیزهایی را در کد لحاظ کنید که «دهان‌پرکن» باشد؟ (فنی)
۱. سیستم Log (گزارش‌گیری): هر اتفاقی که می‌افتد (مثلاً User درخواست داد)، یک جا ذخیره شود تا Admin بتواند تاریخچه را ببیند.
۲. کیف پول مجازی (Wallet): تراکنش‌های مالی واقعی انجام نشود، اما هر کاربر یک عدد balance داشته باشد که با هر معامله کم و زیاد شود.
۳. وضعیت‌های چندگانه (State Machine): هر پروژه وضعیت داشته باشد: Pending -> Accepted -> In Production -> Delivered -> Finished.


انتخاب هوشمندانه‌ای است! پروژه «مدیریت زنجیره تأمین هوشمند» (Smart Supply Chain) به دلیل داشتن روابط منطقی پیچیده، دقیقاً همان چیزی است که استادتان به عنوان «تفکر سیستمی» از شما انتظار دارد. در این سیستم، کوچک‌ترین تغییر در بخش «تأمین»، کل فرآیند «تولید» را تحت تأثیر قرار می‌دهد.
در ادامه، جزئیات کامل این سیستم را برای پیاده‌سازی باز می‌کنیم:
۱. ساختار نقش‌ها (Roles & Access)
باید یک سیستم ورود (Login) داشته باشید که بر اساس نوع کاربر، منوهای متفاوتی را باز کند:

۲. فلوچارت منطقی پروژه (تفکر سیستمی)
برای اینکه کد شما «جون‌دار» باشد، فرآیند باید به این صورت بچرخد:
ورودی: Host قطعه را موجود می‌کند.
فرآیند: User قطعه را رزرو می‌کند -> اعتبار User کم می‌شود -> موجودی انبار Host کم می‌شود.
شرط سیستم: اگر موجودی انبار به صفر رسید، قطعه از لیست Marketplace حذف شود.
خروجی: تولید رسید دیجیتال برای هر دو طرف و آپدیت شدن دیتابیس کل سیستم.



۴. بخش خلاقانه (برای گرفتن نمره تشویقی)
برای اینکه پروژه شما «نماد ورودی ۴۰۴» شود، این دو ویژگی را اضافه کنید:
سیستم رتبه‌بندی (Rating): بعد از هر خرید، User به Host امتیاز بدهد. اگر امتیاز یک Host کمتر از ۳ شد، ادمین پیامی برای بررسی کیفیت او دریافت کند.
داشبورد آماری ساده: در پایان برنامه (یا در پنل ادمین)، سیستم نشان دهد که «پرفروش‌ترین قطعه ماه» چه بوده است. این یعنی شما توانسته‌اید روی داده‌ها تحلیل انجام دهید.
۵. ابزارهای پیشنهادی برای پیاده‌سازی
زبان: پایتون (Python) به دلیل سادگی در پیاده‌سازی دیکشنری‌ها و لیست‌ها برای شبیه‌سازی دیتابیس.
ذخیره‌سازی: اگر می‌توانید از فایل‌های .txt یا .json استفاده کنید تا با هر بار بستن برنامه، اطلاعات پاک نشود (این کار استاد را به شدت تحت تأثیر قرار می‌دهد).
رابط کاربری: فعلاً در سطح کنسول (CLI) با منوهای شماره‌گذاری شده (مثلاً: برای خرید عدد ۱ را بزنید) کفایت می‌کند، مگر اینکه بخواهید با کتابخانه‌هایی مثل CustomTkinter آن را گرافیکی کنید.


حتماً! اضافه کردن بخش «مدیریت مالی و تسهیلات» (Financial & Credit System) دقیقاً همان چیزی است که پروژه شما را از یک کار دانشجویی ساده به یک شبیه‌ساز واقعی صنعتی تبدیل می‌کند. این کار باعث می‌شود «تفکر سیستمی» که استادتان روی آن تأکید داشت، به اوج برسد؛ چون حالا یک تغییر در بخش مالی، مستقیماً روی زنجیره تأمین اثر می‌گذارد.
بیا این بخش‌ها را به پروژه اضافه کنیم تا حسابی «پر و بال» بگیرد:
۱. سیستم درخواست وام و اعتبار (Loan & Credit Flow)
در دنیای واقعی، استارتاپ‌ها همیشه نقدینگی ندارند. شما می‌توانید یک مکانیسم مالی طراحی کنید:
درخواست وام (Loan Request): اگر یک User بخواهد قطعه‌ای بخرد اما موجودی‌اش (Balance) کافی نباشد، سیستم به او پیشنهاد می‌دهد که «درخواست وام» ثبت کند.
بررسی توسط Admin: ادمین لیستی از درخواست‌های وام را می‌بیند. او می‌تواند بر اساس «تاریخچه فعالیت» یا «امتیاز» یوزر، وام را تایید یا رد کند.
تخصیص اعتبار با بهره: ادمین مبلغ را تایید می‌کند و پول به حساب یوزر واریز می‌شود، اما یک متغیر به اسم debt (بدهی) برای یوزر تعریف می‌شود که باید در مراحل بعدی (مثلاً بعد از فروش محصول نهایی‌اش) آن را برگرداند.
۲. سیستم «تولید محصول نهایی» (Value Addition)
برای اینکه یوزرها (استارتاپ‌ها) فقط خریدار نباشند، این منطق را اضافه کنید:
هر استارتاپ یک سری «فرمول تولید» دارد. مثلاً برای تولید یک «برد هوشمند»، یوزر باید ۳ عدد سنسور و ۱ عدد آی‌سی از Host خریده باشد.
وقتی یوزر دکمه «تولید» را می‌زند، قطعات از انبار مجازی‌اش کم شده و یک محصول جدید (با ارزش بالاتر) به موجودی‌اش اضافه می‌شود.
حالا یوزر می‌تواند این محصول نهایی را در بازار بفروشد تا بدهی وامش را بدهد. این یعنی یک چرخه کامل اقتصادی!
۳. سیستم اولویت‌بندی و مزایده (Bidding System)
برای بخش صنعتی، گاهی یک قطعه «کمیاب» است (مثلاً فقط یک Host آن را دارد و ۱۰ تا User آن را می‌خواهند):
Host می‌تواند قطعه را در حالت «مزایده» (Auction) قرار دهد.
یوزرها قیمت‌های پیشنهادی خود را ثبت می‌کنند.
در پایان زمان معین، سیستم به صورت خودکار قطعه را به بالاترین قیمت می‌فروشد و ادمین هم درصدی از این معامله برمی‌دارد.
۴. ساختار دیتابیس پیشنهادی (به زبان ساده)
برای اینکه یادتان نرود چه چیزی را ذخیره کنید، این مدل داده‌ای (Data Model) را در نظر بگیرید:




عکس











سناریوی ارائه به استاد (یک مثال واقعی)
در روز ارائه، این سناریو را اجرا کنید:
ابتدا: یک Host وارد می‌شود و ۵ عدد سنسور کمیاب را موجود می‌کند.
سپس: یک User وارد می‌شود و می‌بیند پولش کم است. درخواست وام ۱۰۰۰ دلاری می‌دهد.
حرکت ادمین: شما در نقش ادمین، پنل را باز می‌کنید، درخواست را می‌بینید و به دلیل اینکه این یوزر قبلاً خوش‌حساب بوده، وام را تایید می‌کنید.
نهایی کردن معامله: حالا یوزر با همان پول وام، سنسور را از Host می‌خرد. موجودی Host زیاد می‌شود، موجودی یوزر کم (و بدهی‌اش زیاد) و انبار Host هم آپدیت می‌شود.
نکته فنی: برای پیاده‌سازی این‌ها، از دیکشنری‌های تو در تو (Nested Dictionaries) در پایتون استفاده کنید. مثلاً یک دیکشنری برای تمام کاربران که کلیدش نام کاربری و مقدارش یک دیکشنری از ویژگی‌های اوست.
چقدر با پایتون (یا زبان انتخابی‌تان) راحت هستید؟
اگر بخواهید، می‌توانم اسکلت‌بندی اصلی کد (Main Structure) را برایتان بنویسم که توابع وام و خرید در آن مشخص شده باشد تا فقط داخلش را پر کنید. مایلید؟

ویژگی‌های کلیدی این طراحی برای ارائه به استاد:
ارتباط متقابل (Interconnectivity): خرید یوزر باعث پولدار شدن هاست و کاهش موجودی کل می‌شود (تفکر سیستمی).
چرخه مالی (Financial Cycle): وام گرفته شده از ادمین، صرف خرید از هاست می‌شود، سپس با تولید محصول نهایی و فروش آن، یوزر می‌تواند بدهی‌اش را به ادمین برگرداند.
مدیریت نقش‌ها: برنامه با یک منوی اصلی شروع می‌شود: 1. Admin Panel, 2. Host Panel, 3. User Panel.
شفافیت کد: توابع کاملاً تفکیک شده‌اند (Modular Coding) که استاد به شدت روی آن تأکید داشت.
پیشنهاد برای مرحله بعد:
اگر موافقی، می‌توانیم روی "بخش گزارش‌گیری" (Logging) کار کنیم؛ یعنی ادمین بتواند فایلی را باز کند که در آن نوشته شده: "در ساعت ۱۲، یوزر X قطعه Y را از هاست Z خرید". این یعنی سیستم شما حافظه دارد.
آیا دوست داری کد کامل و یکپارچه این بخش‌ها را برای کپی کردن در ادیتور برایت بفرستم؟



فنی پروژه "نِکسوس صنعتی ۴۰۴" (Industrial Nexus 404)
۱. مقدمه و هدف سیستم
این پروژه یک پلتفرم هوشمند برای مدیریت زنجیره تأمین قطعات بین استارتاپ‌های تولیدی و تأمین‌کنندگان قطعات صنعتی است. هدف اصلی، مدل‌سازی چرخه «تأمین، اعتباردهی مالی و تولید» است که در آن جریان نقدینگی و موجودی کالا به صورت لحظه‌ای رصد می‌شود.
۲. معماری نقش‌ها و دسترسی‌ها (User Hierarchy)
سیستم از سه سطح دسترسی مجزا تشکیل شده است که هر کدام «محیط عملیاتی» (Sandbox) مخصوص به خود را دارند:
الف) مدیر سیستم (The Admin)
ادمین در این سیستم نقش بانک مرکزی و ناظر کیفی را ایفا می‌کند.
تخصیص اعتبار (Loan Management): بررسی درخواست‌های وام استارتاپ‌ها بر اساس میزان ریسک و سوابق.
مانیتورینگ زیرساخت: مشاهده کل نقدینگی در گردش و شناسایی گره‌های زنجیره تأمین (قطعاتی که موجودی‌شان رو به اتمام است).
حل اختلاف: تأیید نهایی تراکنش‌های بزرگ مالی.
ب) میزبان یا تأمین‌کننده (The Host)
هاست‌ها در نقش انبارداران و فروشندگان عمده عمل می‌کنند.
مدیریت موجودی (Inventory Management): ورود قطعات به سیستم، تعیین قیمت بر اساس تقاضا و به‌روزرسانی تعداد کالا.
تحلیل فروش: مشاهده گزارش درآمدها و لیست خریدارانی که بیشترین تعامل را با آن‌ها داشته‌اند.
ج) کاربر یا استارتاپ (The User)
یوزرها هسته مولد سیستم هستند که با ورودی قطعات، خروجی باارزش تولید می‌کنند.
خرید هوشمند: جستجو در انبار تمام هاست‌ها برای یافتن ارزان‌ترین قطعه.
مدیریت مالی: ثبت درخواست وام در صورت کسری بودجه.
واحد تولید (Value-Added Unit): تبدیل قطعات خام به «محصول نهایی» جهت فروش و سودآوری.
۳. فرآیندهای سیستمی (System Workflows)
چرخه خرید و تأمین (The Procurement Cycle)
یوزر درخواست خرید یک قطعه را ثبت می‌کند.
سیستم اعتبارسنجی می‌کند:
اگر پول کافی باشد: کسر از حساب یوزر -> واریز به حساب هاست -> کسر از موجودی انبار.
اگر پول کافی نباشد: هدایت یوزر به زیرسیستم درخواست وام.
چرخه وام و بازپرداخت (Financial Flow)
درخواست وام در صف انتظار (Pending) قرار می‌گیرد. با تأیید ادمین، مبلغ به «موجودی جاری» یوزر اضافه شده و معادل آن در بخش «بدهی» (Debt) ثبت می‌شود. یوزر موظف است پس از تولید و فروش محصول، بدهی را به خزانه ادمین بازگرداند.
۴. ویژگی‌های تفکر سیستمی در پروژه
این برنامه صرفاً یک برنامه ثبت اطلاعات نیست؛ بلکه یک موجود زنده دیجیتال است زیرا:
اثر پروانه‌ای: کاهش موجودی در انبار یک هاست، بلافاصله روی امکان تولید در پنل یوزر تأثیر می‌گذارد.
پویایی (Dynamics): قیمت‌ها و موجودی‌ها ثابت نیستند و بر اساس تراکنش‌ها تغییر می‌کنند.
بازخورد (Feedback Loop): سیستم رتبه‌بندی باعث می‌شود هاست‌های ضعیف به مرور از گردونه رقابت حذف شوند.
۵. جزئیات پیاده‌سازی فنی
مدیریت وضعیت (State Management): استفاده از ساختارهای داده‌ای تو در تو برای حفظ یکپارچگی اطلاعات بین سه نقش.
ماژولار بودن: هر بخش (مالی، انبار، تولید) در توابع کاملاً مجزا نوشته شده تا توسعه آن در آینده آسان باشد.
پایداری (Stability): سیستم در برابر ورودی‌های غیرمجاز (مانند خرید تعداد بیش از موجودی) مقاوم است.
گام بعدی برای تیم شما:
این مستندات به شما کمک می‌کند تا در روز ارائه، به جای صحبت درباره «کد»، درباره «منطق بیزینس و سیستم» صحبت کنید.
آیا می‌خواهید بخش «سناریوی تست» (مثلاً چطور سیستم را تست کنیم که باگ نداشته باشد) را هم به این داکیومنت اضافه کنم؟


<!--
**unic86/unic86** is a ✨ _special_ ✨ repository because its `README.md` (this file) appears on your GitHub profile.

Here are some ideas to get you started:

- 🔭 I’m currently working on ...
- 🌱 I’m currently learning ...
- 👯 I’m looking to collaborate on ...
- 🤔 I’m looking for help with ...
- 💬 Ask me about ...
- 📫 How to reach me: ...
- 😄 Pronouns: ...
- ⚡ Fun fact: ...
-->
